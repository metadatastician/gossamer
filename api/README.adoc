// SPDX-License-Identifier: CC-BY-SA-4.0
= API
Public API surface for gossamer, currently exposing a zig binding (`v/gossamer.v`).
This directory tracks the versioned stable interface that downstream consumers depend on; each subdirectory corresponds to a major API version.
