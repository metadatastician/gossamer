# SPDX-License-Identifier: MPL-2.0
# TEMPORARY probe (lockfile diagnostics) — removed before the final PR.
name: ZZ Probe B

on:
  push:
    branches: ['arena/**']
    paths:
      - 'scripts/hypatia-snapshot-trigger.txt'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7.0.1
      - name: No-op
        run: echo "probe B ran"
