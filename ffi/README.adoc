// SPDX-License-Identifier: PMPL-1.0-or-later
= FFI
Zig FFI implementation providing the C-compatible ABI bridge between the Idris2 interface definitions and the native WebView platform code.
The `zig/` subdirectory contains the Zig build unit; generated C headers consumed here live in `generated/`.
