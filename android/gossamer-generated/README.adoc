// SPDX-License-Identifier: MPL-2.0
= Generated — Android JVM shims
:toc: macro

Auto-generated by `tools/android-codegen` — *do not edit by hand*. Regenerate with `just android-gen`.

This tree is the JVM-bytecode boundary for gossamer's Android target. It is produced by gossamer's own tooling (per estate policy: unavoidable JVM shims are generated, never hand-authored, never pushed onto downstream apps). Every native method here has a matching `export fn Java_io_gossamer_*` in `src/interface/ffi/src/` — the symbol correspondence is checked in the PR that introduced these files.

== Contents

`java/io/gossamer/`::
* `GossamerActivity.java` — WebView host `Activity`. Calls `nativeInit(Activity, WebView, int, int)` / `nativeDestroy()`.
* `GossamerBridge.java` — `@JavascriptInterface` IPC bridge. `postMessage(json)` → `nativePostMessage`.
* `GossamerService.java` — foreground `Service`. Lifecycle → native; applies the foreground/sticky directive.
* `GossamerReceiver.java` — `BroadcastReceiver`. `onReceive` → native; executes start-service/log directives.
* `GossamerWidget.java` — `AppWidgetProvider`. `onUpdate`/`onReceive` → native; applies the RemoteViews render directive.

`AndroidManifest.template.xml`::
Manifest skeleton declaring the four hosts and the permissions they need. Copy into the app module and fill the remaining app-specific entries (or pass `package`/`label`/`launcherActivity`/`widgetInfoRes` to `just android-gen`).

== How an app uses these

A downstream app does NOT subclass these in Kotlin/Java. It links `libgossamer.so`, registers pure handlers (Rust/Zig/Idris) via `gossamer_service_bind` / `gossamer_receiver_bind` / `gossamer_widget_bind`, and ships this generated tree as-is. See `docs/architecture/android-components.adoc`.
